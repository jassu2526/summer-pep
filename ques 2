def minTrips(weight):
    weight.sort()

    i = 0
    j = len(weight) - 1
    trips = 0

    while i <= j:
        if weight[i] + weight[j] <= 3:
            i += 1
        j -= 1
        trips += 1

    return trips


# Sample Input
weight = [1.01, 1.99, 2.5, 1.5, 1.01]

print(minTrips(weight))